var mongoose = require('mongoose')
  , Schema = mongoose.Schema;

var ShapeArr = mongoose.model('ShapeArr', new Schema({
    agency_key        :  { type: String, index: true }
  , shape_id          :  { type: Number, index: true }
  , shape_pt_arr      :  []
}, { strict: true }));